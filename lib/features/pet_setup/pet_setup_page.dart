import 'package:flutter/material.dart';

/// 펫 등록 화면 스텁. Phase 5에서 실제 UI로 대체한다.
class PetSetupPage extends StatelessWidget {
  const PetSetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('펫 등록')),
      body: const Center(child: Text('펫 등록')),
    );
  }
}
